# ADR 0003 — Canonical Structured Artifacts

## Decision
Graph, trace, scene, and lesson artifacts are stored as versioned structured JSON. HTML and SVG are render outputs.

## Rationale
Structured artifacts support validation, reuse, migration, multiple renderers, and reproducible publication.
