Hand analysis: function `calculate_total` with param `prices`, state binding `total`
initialized to 0, loop over prices, operation adds price to total, return total.
IDs are illustrative for P0 schema scaffolding.
