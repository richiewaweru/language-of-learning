# Wave A Conflicts

No unresolved governing-document conflicts were found.

A07 uses the corpus-authorized replacement for empty-list truthiness. The pre-existing `array-update` snapshot mismatch was formatting-only and was regenerated from the canonical analyzer.
